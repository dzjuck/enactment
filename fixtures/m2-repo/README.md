# M2 tests-first fixture

This fixture remains because Docker tests use `createM2Repo()`.

For the supported end-to-end example, use [`demo/`](../../demo/README.md). It includes a
credential-free replay and live-provider instructions.
