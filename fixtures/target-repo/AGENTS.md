# Agent instructions

Repository-supplied guidance. Trusted content: it is exported into the workspace and is
expected to influence the agent.

- Source lives in `src/`, tests in `test/`.
- Do not add dependencies.
