/**
 * Convert a title into a URL-safe slug.
 *
 * Deliberately unimplemented: this is the function the harness asks an agent to write.
 *
 * @param {string} title
 * @returns {string}
 */
export function slugify(title) {
  throw new Error(`slugify is not implemented (called with ${title})`);
}
