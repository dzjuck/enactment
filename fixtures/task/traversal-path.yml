id: add-slugify
prompt: Implement the slugify function in src/slugify.js.
implementation_paths:
  - src/../../outside/slugify.js
verification:
  commands:
    - ["npx", "--no-install", "vitest", "run"]
